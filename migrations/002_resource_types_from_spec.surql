-- ============================================================
-- AUTO-GENERATED by @kdl-schema/codegen-surql — do not edit by hand.
-- Regenerate when the source KDL spec changes.
-- ============================================================

-- user
DEFINE TABLE OVERWRITE user TYPE NORMAL SCHEMAFULL;

DEFINE FIELD OVERWRITE handle ON user TYPE string
  ASSERT $value != NONE
  PERMISSIONS FULL;

DEFINE FIELD OVERWRITE email ON user TYPE string
  ASSERT $value != NONE
  COMMENT '個人なら user email、 org なら管理 email'
  PERMISSIONS FULL;

DEFINE FIELD OVERWRITE display_name ON user TYPE option<string>
  PERMISSIONS FULL;

DEFINE FIELD OVERWRITE avatar_url ON user TYPE option<string>
  PERMISSIONS FULL;

DEFINE FIELD OVERWRITE bio ON user TYPE option<string>
  PERMISSIONS FULL;

DEFINE FIELD OVERWRITE locale ON user TYPE option<string>
  PERMISSIONS FULL;

DEFINE FIELD OVERWRITE account_type ON user TYPE string
  ASSERT $value != NONE
  PERMISSIONS FULL;

-- organization
DEFINE TABLE OVERWRITE organization TYPE NORMAL SCHEMAFULL;

DEFINE FIELD OVERWRITE owner_user ON organization TYPE option<string>
  COMMENT 'org 作成者 (transfer 可能)'
  PERMISSIONS FULL;

DEFINE FIELD OVERWRITE billing_email ON organization TYPE option<string>
  PERMISSIONS FULL;

DEFINE FIELD OVERWRITE billing_address ON organization TYPE option<string>
  PERMISSIONS FULL;

DEFINE FIELD OVERWRITE verified ON organization TYPE option<bool>
  PERMISSIONS FULL;

DEFINE FIELD OVERWRITE logo_url ON organization TYPE option<string>
  PERMISSIONS FULL;

DEFINE FIELD OVERWRITE founded_at ON organization TYPE option<string>
  PERMISSIONS FULL;

-- org-membership
DEFINE TABLE OVERWRITE org_membership TYPE NORMAL SCHEMAFULL;

DEFINE FIELD OVERWRITE org_user ON org_membership TYPE option<string>
  PERMISSIONS FULL;

DEFINE FIELD OVERWRITE member_user ON org_membership TYPE option<string>
  PERMISSIONS FULL;

DEFINE FIELD OVERWRITE role ON org_membership TYPE string
  ASSERT $value != NONE
  PERMISSIONS FULL;

DEFINE FIELD OVERWRITE joined_at ON org_membership TYPE string
  ASSERT $value != NONE
  PERMISSIONS FULL;

DEFINE FIELD OVERWRITE invited_by ON org_membership TYPE option<string>
  PERMISSIONS FULL;

-- notification
DEFINE TABLE OVERWRITE notification TYPE NORMAL SCHEMAFULL;

DEFINE FIELD OVERWRITE from_product ON notification TYPE string
  ASSERT $value != NONE
  PERMISSIONS FULL;

DEFINE FIELD OVERWRITE from_resource ON notification TYPE option<string>
  PERMISSIONS FULL;

DEFINE FIELD OVERWRITE kind ON notification TYPE option<string>
  PERMISSIONS FULL;

DEFINE FIELD OVERWRITE title ON notification TYPE string
  ASSERT $value != NONE
  PERMISSIONS FULL;

DEFINE FIELD OVERWRITE body ON notification TYPE option<string>
  PERMISSIONS FULL;

DEFINE FIELD OVERWRITE action_url ON notification TYPE option<string>
  PERMISSIONS FULL;

DEFINE FIELD OVERWRITE read_at ON notification TYPE option<string>
  PERMISSIONS FULL;

-- follow
DEFINE TABLE OVERWRITE follow TYPE NORMAL SCHEMAFULL;

DEFINE FIELD OVERWRITE follower ON follow TYPE string
  ASSERT $value != NONE
  PERMISSIONS FULL;

DEFINE FIELD OVERWRITE followee ON follow TYPE string
  ASSERT $value != NONE
  PERMISSIONS FULL;

DEFINE FIELD OVERWRITE started_at ON follow TYPE string
  ASSERT $value != NONE
  PERMISSIONS FULL;

-- memories-atlas
DEFINE TABLE OVERWRITE memories_atlas TYPE NORMAL SCHEMAFULL;

DEFINE FIELD OVERWRITE atlas_id ON memories_atlas TYPE string
  ASSERT $value != NONE
  COMMENT 'EntId (creo-memories 側 primary key)'
  PERMISSIONS FULL;

DEFINE FIELD OVERWRITE title ON memories_atlas TYPE string
  ASSERT $value != NONE
  PERMISSIONS FULL;

DEFINE FIELD OVERWRITE description ON memories_atlas TYPE option<string>
  PERMISSIONS FULL;

DEFINE FIELD OVERWRITE memory_count ON memories_atlas TYPE option<int>
  PERMISSIONS FULL;

DEFINE FIELD OVERWRITE visibility ON memories_atlas TYPE option<string>
  PERMISSIONS FULL;

-- vp-world
DEFINE TABLE OVERWRITE vp_world TYPE NORMAL SCHEMAFULL;

DEFINE FIELD OVERWRITE world_id ON vp_world TYPE string
  ASSERT $value != NONE
  PERMISSIONS FULL;

DEFINE FIELD OVERWRITE title ON vp_world TYPE string
  ASSERT $value != NONE
  PERMISSIONS FULL;

DEFINE FIELD OVERWRITE description ON vp_world TYPE option<string>
  PERMISSIONS FULL;

DEFINE FIELD OVERWRITE tmux_session_name ON vp_world TYPE option<string>
  PERMISSIONS FULL;

DEFINE FIELD OVERWRITE actors ON vp_world TYPE option<array<any>>
  PERMISSIONS FULL;

-- vp-actor
DEFINE TABLE OVERWRITE vp_actor TYPE NORMAL SCHEMAFULL;

DEFINE FIELD OVERWRITE actor_id ON vp_actor TYPE string
  ASSERT $value != NONE
  COMMENT 'EntId vpa_...'
  PERMISSIONS FULL;

DEFINE FIELD OVERWRITE stand ON vp_actor TYPE option<string>
  COMMENT 'Stand Ensemble role'
  PERMISSIONS FULL;

DEFINE FIELD OVERWRITE lane ON vp_actor TYPE option<string>
  COMMENT 'lane id'
  PERMISSIONS FULL;

DEFINE FIELD OVERWRITE canonical_name ON vp_actor TYPE option<string>
  COMMENT 'agent@creo-memories 形式'
  PERMISSIONS FULL;

DEFINE FIELD OVERWRITE world_id ON vp_actor TYPE option<string>
  COMMENT 'vp-world への FK'
  PERMISSIONS FULL;

DEFINE FIELD OVERWRITE active ON vp_actor TYPE option<bool>
  PERMISSIONS FULL;

-- cplp-session
DEFINE TABLE OVERWRITE cplp_session TYPE NORMAL SCHEMAFULL;

DEFINE FIELD OVERWRITE session_id ON cplp_session TYPE string
  ASSERT $value != NONE
  PERMISSIONS FULL;

DEFINE FIELD OVERWRITE title ON cplp_session TYPE string
  ASSERT $value != NONE
  PERMISSIONS FULL;

DEFINE FIELD OVERWRITE started_at ON cplp_session TYPE string
  ASSERT $value != NONE
  PERMISSIONS FULL;

DEFINE FIELD OVERWRITE ended_at ON cplp_session TYPE option<string>
  PERMISSIONS FULL;

DEFINE FIELD OVERWRITE participants ON cplp_session TYPE option<array<any>>
  PERMISSIONS FULL;

DEFINE FIELD OVERWRITE bpm ON cplp_session TYPE option<int>
  PERMISSIONS FULL;

DEFINE FIELD OVERWRITE key ON cplp_session TYPE option<string>
  PERMISSIONS FULL;

-- fleetstage-tenant
DEFINE TABLE OVERWRITE fleetstage_tenant TYPE NORMAL SCHEMAFULL;

DEFINE FIELD OVERWRITE tenant_id ON fleetstage_tenant TYPE string
  ASSERT $value != NONE
  PERMISSIONS FULL;

DEFINE FIELD OVERWRITE title ON fleetstage_tenant TYPE string
  ASSERT $value != NONE
  PERMISSIONS FULL;

DEFINE FIELD OVERWRITE fleet_count ON fleetstage_tenant TYPE option<int>
  PERMISSIONS FULL;

DEFINE FIELD OVERWRITE region ON fleetstage_tenant TYPE option<string>
  PERMISSIONS FULL;

-- gfp-shipment
DEFINE TABLE OVERWRITE gfp_shipment TYPE NORMAL SCHEMAFULL;

DEFINE FIELD OVERWRITE shipment_id ON gfp_shipment TYPE string
  ASSERT $value != NONE
  PERMISSIONS FULL;

DEFINE FIELD OVERWRITE origin ON gfp_shipment TYPE option<string>
  PERMISSIONS FULL;

DEFINE FIELD OVERWRITE destination ON gfp_shipment TYPE option<string>
  PERMISSIONS FULL;

DEFINE FIELD OVERWRITE status ON gfp_shipment TYPE option<string>
  PERMISSIONS FULL;

DEFINE FIELD OVERWRITE eta ON gfp_shipment TYPE option<string>
  PERMISSIONS FULL;

-- app
DEFINE TABLE OVERWRITE app TYPE NORMAL SCHEMAFULL;

DEFINE FIELD OVERWRITE app_id ON app TYPE string
  ASSERT $value != NONE
  PERMISSIONS FULL;

DEFINE FIELD OVERWRITE name ON app TYPE string
  ASSERT $value != NONE
  PERMISSIONS FULL;

DEFINE FIELD OVERWRITE description ON app TYPE option<string>
  PERMISSIONS FULL;

DEFINE FIELD OVERWRITE icon_url ON app TYPE option<string>
  PERMISSIONS FULL;

DEFINE FIELD OVERWRITE home_url ON app TYPE option<string>
  PERMISSIONS FULL;

DEFINE FIELD OVERWRITE scopes ON app TYPE option<array<any>>
  PERMISSIONS FULL;

DEFINE FIELD OVERWRITE verified ON app TYPE option<bool>
  PERMISSIONS FULL;

DEFINE FIELD OVERWRITE manifest_version ON app TYPE option<string>
  COMMENT '準拠 spec version (G6)'
  PERMISSIONS FULL;

DEFINE FIELD OVERWRITE manifest_url ON app TYPE string
  ASSERT $value != NONE
  PERMISSIONS FULL;

DEFINE FIELD OVERWRITE public_key_jwks_uri ON app TYPE option<string>
  PERMISSIONS FULL;

DEFINE FIELD OVERWRITE status ON app TYPE option<string>
  PERMISSIONS FULL;
